<div class="reviews-setup">
    <header>
        <div class="container">
            <div class="row">
                <div class="text-center col-xs-12">
                    <h1>Choose moderation settings for {{model.name}}</h1>
                </div>
            </div>
        </div>
    </header>

    <div class="text-bigger alert alert-warning text-center">
        <i class="fa fa-exclamation-triangle" aria-hidden="true"></i>
        Once finalized, moderation settings can only be changed by an OSF administrator.
    </div>

    <div class="container">
        <div class="row m-t-lg">
            <form class="col-md-offset-2 col-md-8 col-sm-offset-1 col-sm-10">
                {{#each settingsOptions as |option|}}
                    {{#form-block title=option.title description=option.description}}
                        {{#each option.options as |choice|}}
                            {{form-block-radiobutton
                            title=choice.title
                            description=choice.description
                            name=option.name
                            value=choice.value
                            attribute=(mut (get this option.name))
                            }}
                        {{/each}}
                    {{/form-block}}
                {{/each}}
            </form>
        </div>

        <br>

        <div class="row">
            <div class="col-xs-offset-2 col-xs-8">
                <div class="btn-toolbar pull-right">
                    <button class="btn btn-default" {{action "cancel"}}>Cancel</button>
                    <button class="btn btn-success" {{action "submit"}}>Finalize Settings</button>
                </div>
            </div>
        </div>
    </div>
</div>

